## ADDED Requirements

### Requirement: Example requirement
The system SHALL provide an example requirement so the reader has structured
spec content to display.

#### Scenario: Viewing the example
- **WHEN** the user opens this spec in LecternApp
- **THEN** the requirement and its scenario render as formatted markdown
