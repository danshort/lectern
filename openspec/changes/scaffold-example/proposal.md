## Why

A scaffolded, non-functional example change used to preview **LecternApp** with
more than one active change in the Active Changes list. It implements nothing.

## What Changes

- Adds a sample capability spec, design notes, and a small task list purely so
  the macOS reader has a second change to render (sidebar grouping, artifacts,
  markdown, tasks, and a delta spec with a validation-clean structure).

## Non-goals

- Any real behavior change to lectern. This change is a demo fixture and should
  be removed before the macOS-app work merges to `main`.

## Capabilities

### New Capabilities

- `example-capability`: a placeholder capability that exists only to give the
  reader a spec to display.

## Impact

- `openspec/changes/scaffold-example/**` — demo artifacts only. No code.
