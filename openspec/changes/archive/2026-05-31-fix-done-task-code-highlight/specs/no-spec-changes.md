## No spec changes

This change is an implementation fix only — no spec-level requirement changes. The visual behavior of done-task code spans (underlined, dim foreground) remains identical to the intended design; only the rendering approach is corrected to match.
